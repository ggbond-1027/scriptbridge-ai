# NovelScripter 后端依赖
# 核心运行依赖（必须）
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-core==2.18.4
pydantic-settings==2.3.4
python-dotenv==1.0.1
python-multipart==0.0.9
httpx==0.27.0
aiofiles==23.2.1
openai==1.35.5
chardet==5.2.0
strictyaml==1.1.1
ruamel.yaml==0.18.6
websockets==12.0

# LLM 结构化输出
instructor==1.3.3

# LangChain（可选，Pipeline 编排）
langchain==0.2.6
langchain-core==0.2.11
langchain-openai==0.1.14

# 数据库（PostgreSQL，可选）
sqlalchemy==2.0.31
psycopg2-binary==2.9.9
alembic==1.13.1

# Redis + Celery（异步任务，可选）
redis==5.0.7
celery==5.4.0

# MinIO（文件存储，可选）
minio==7.2.7

# PDF 导出（可选，Windows 需要 GTK 运行时）
# 如需 PDF 导出功能，请单独安装：pip install weasyprint
# weasyprint==62.2

# 认证（可选）
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# 测试
pytest==8.2.2
pytest-asyncio==0.23.7